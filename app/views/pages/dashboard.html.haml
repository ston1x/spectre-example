.container
  .row
    .col-sm
      .card{"style" => "18rem;"}
        .card-body
          %h5.card-title
            Logins
          %h6.card-subtitle.mb-2.text-muted
            customer_id: #{current_user.customer_id}
            - if current_user.customer_id.nil?
              = link_to 'create', create_customer_path(current_user), method: :post, class: 'card-link'
          %h6.card-subtitle.mb-2.text-muted
            - if current_user.logins.nil?
              = link_to 'create', create_login_path(current_user), method: :post, class: 'card-link'
            - else
              logins: #{current_user.logins.pluck('login_id')}
          %p.card-text
            The login resource represents a single connection of the client’s customer to a source of financial data – one of the providers.
          - if current_user.logins.any?
            = link_to 'List', logins_path, class: 'card-link'
            = link_to 'Refresh', refresh_login_path, method: :put, class: 'card-link'
            = link_to 'Reconnect', login_credentials_path, class: 'card-link'
            = link_to 'Destroy', '#', class: 'card-link'

    .col-sm
      .card{"style" => "18rem;"}
        .card-body
          %h5.card-title
            Accounts
          %p.card-text
            Some quick example text to build on the card title and make up the bulk of the card's content.
          = link_to 'Fetch', fetch_accounts_path, class: 'card-link'

    .col-sm
      .card{"style" => "18rem;"}
        .card-body
          %h5.card-title
            Transactions
          %p.card-text
            Some quick example text to build on the card title and make up the bulk of the card's content.
          = link_to 'Fetch', fetch_transactions_path, class: 'card-link'